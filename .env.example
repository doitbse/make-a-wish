# URL of the companion Gemini triage service (private repo: doitbse/make-a-wish-agent).
# When unset, submissions are still stored locally but not triaged. Server-side only.
TRIAGE_SERVICE_URL=

# Set to 1 to render the triage agent's verdict in the widget after submit (debug/demo).
NEXT_PUBLIC_FEEDBACK_DEBUG=1
